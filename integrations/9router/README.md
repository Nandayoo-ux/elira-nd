# 9Router Integration

Planned provider adapter. Keep the model layer OpenAI-compatible so ELARA can route tasks through the local 9Router instance without coupling the harness core to a single vendor.
