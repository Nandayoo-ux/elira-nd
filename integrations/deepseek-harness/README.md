# DeepSeek Harness Integration

ELARA treats DeepSeek Harness as the single harness runtime. The upstream repository is fetched by `scripts/bootstrap-local.ps1` and kept under `.runtime/` rather than vendored into ELARA.
